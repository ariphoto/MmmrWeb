extends ../layout_template/layout

block content
    title パスワード
    if (message)
        if (message)
            .alert.alert-danger(role="alert") #{message}

    p(style={color: "red", "font-weight": "bolder", "font-size": "xx-large"}) パスワード変更

    p
    br
    |変更用のリンクを送信しますのでメールアドレスの記入後下の「送信」クリックをお願いします

    form#forgotPassword_form(method="POST" action="/forgotPassword/inputAddress")
        table(class='type1')
            tr
                td
                    |保育園ID
                td
                    input(name='userID',required)
            tr
                td
                    |メールアドレス
                td
                    input(name='userURL',required)
        p: button(type='submit') 送信