table
    tr
        td 保育園名
        td
            input#id_h(type="hidden" name='id' value = data.schoolId)
            input#schoolNameBox(type='text', name='name', size=20 value=data.name)
    tr
        td 保育園ID
        td: input#schoolIdBox(type='text', name='name', size=20)
    tr
        td メールアドレス
        td: input#schoolAddressBox(type='text', name='name', size=20 value=data.mailAddress)
    tr
        td 新規パスワード
        td: input#schoolPassBox(type='password', name='name', size=20)
    tr
        td  新規パスワード（確認）
        td: input#schoolPassConfBox(type='password', name='name', size=20)